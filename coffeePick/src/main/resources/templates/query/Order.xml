<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cp.user.order.dao.OrderDAO">
	<select id="orderList" parameterType="order" resultType="order">
        select
            oh.order_regdate,
            oh.order_total_price,
            od.order_detail_menu_name
        from order_history as oh
        join order_detail as od on oh.order_no = od.order_no
        where oh.order_no = #{order_no} <!-- 주문 번호를 파라미터로 받음 --> 
	</select>
</mapper>